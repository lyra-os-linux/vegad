# LogsDirectory=vega in vegad-log-export.service creates the directory and
# migrates existing root-owned logs together. Do not change just the directory
# owner here: systemd would then skip the recursive ownership migration.
